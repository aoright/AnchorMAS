# Backend

This workspace is reserved for the backend service that connects the product frontend to runtime services.

It is intentionally separate from `agent/`, which contains the Rust MAS agent runtime, harvesting pipeline, analysis pipeline, briefing APIs, and monitoring APIs.
