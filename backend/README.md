# Backend

This workspace documents the backend service that connects the product frontend to runtime services.

The current implementation lives in `agent/src/web/app_handlers.rs` and exposes `/app/*` routes for the product frontend.

See [API_DOC.md](./API_DOC.md) for the frontend-facing API contract.
